# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/credify

# Server Configuration
PORT=5001
NODE_ENV=development

# Firebase Admin SDK
# Option 1: Path to service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option 2: Use Google Application Credentials (recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# Email Configuration (Gmail)
# To generate app password: https://myaccount.google.com/apppasswords
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# SMS Configuration - Twilio (Required for sending SMS to phone numbers)
# Sign up at https://www.twilio.com/try-twilio
# Get your Account SID, Auth Token, and phone number from Twilio Console
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Note: For Firebase Cloud Messaging (push notifications), users must register
# their FCM token via the mobile app using POST /api/users/register-fcm-token

# Legacy JWT Configuration (no longer needed with Firebase Auth)
# JWT_SECRET=your_jwt_secret_key_here
# JWT_EXPIRE=30d
